/**
 * A string that uniquely identifies a profile.
 */
export declare type ProfileHandle = string;
/**
 * A string that uniquely identifies a key.
 * These are keys to the "keys" object store.
 */
export declare type KeyHandle = string;
/**
 * A string that uniquely identifies a certificate.
 * These are keys to the "certificates" object store.
 */
export declare type CertHandle = string;
